server:
  port: ${SERVER_PORT:8083}

spring:
  data:
    mongodb:
      uri: ${TRACEABILITY_MONGODB_URI:${MONGODB_URI:mongodb://localhost:27017/duriancare_traceability}}
      auto-index-creation: true
